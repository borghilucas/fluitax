# Seed data

O script `scripts/seed.js` não cria mais empresas fictícias automaticamente.

Para popular o banco com dados iniciais, crie um arquivo `companies.json` nesta pasta
ou defina a variável de ambiente `SEED_COMPANIES` com um array JSON. Veja o arquivo
`companies.example.json` como referência.
